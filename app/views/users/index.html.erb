<!-- テンプレート化する -->
<h1>User info</h1>
<table>
  <tr>
    <th>name</th>
    <th>introduction</th>
  </tr>
  <tr>
    <td><%= current_user.name%></td>
    <td><%= current_user.introduction%></td>
  </tr>
</table>
<%= link_to "edit", edit_user_path(current_user.id) %>



<p>users</p>
<!-- テンプレート化する -->
<%= form_with model: @book do |f| %>
  <h2>New book</h2>
  <p>Title</p>
  <%= f.text_field :title %><!--モデル名をもとにdbにアクセス -->
  <p>Opinion</p>
  <%= f.text_area :body %>
  <%= f.submit 'Create Book' %>
<% end %>
<!----------------------------->

<table>
  <tr>
    <th>image</th>
    <th>name</th>
  </tr>
  <% @users.each do |user| %><!-- インスタンス変数books -->
    <tr>
      <td></td>
      <td><%= user.name %></td>
      <td><%= link_to "Show", user_path(user.id) %></td>
    </tr>
  <% end %>
</table>